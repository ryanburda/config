#!/bin/zsh

# NAME
#   toggle-transparency
#
# DESCRIPTION
#   Flips the saved transparency default and applies it. A convenience over
#   `set_transparency`, where a two state setting does not need a picker.

if [[ $(set_transparency -d) == 'on' ]]; then
    set_transparency off
else
    set_transparency on
fi
