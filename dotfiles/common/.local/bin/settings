#!/bin/bash

choice=$(printf 'colorscheme\nfont\ntransparency' | fzf --cycle)

case "$choice" in
    colorscheme) set_colorscheme ;;
    font) set_font ;;
    transparency) toggle-transparency ;;
esac
