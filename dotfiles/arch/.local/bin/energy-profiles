#!/bin/zsh

current=$(powerprofilesctl get)
option=$(printf "power-saver\nbalanced\nperformance" | fuzzel --dmenu --prompt "" --mesg "Current: $current")
[ -z "$option" ] && exit 0
powerprofilesctl set "$option"
